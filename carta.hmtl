<!DOCTYPE html><html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Pra Você 💖</title>
  <style>
    * {
      box-sizing: border-box;
      font-family: 'Segoe UI', sans-serif;
    }body {
  margin: 0;
  min-height: 100vh;
  background: linear-gradient(135deg, #ff9a9e, #fad0c4);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px;
}

.card {
  background: #fff;
  max-width: 420px;
  width: 100%;
  border-radius: 20px;
  padding: 30px 25px;
  text-align: center;
  box-shadow: 0 15px 40px rgba(0,0,0,0.15);
  animation: fadeIn 1.5s ease;
}

h1 {
  color: #e63946;
  margin-bottom: 10px;
}

p {
  color: #555;
  line-height: 1.6;
  margin: 15px 0;
}

.heart {
  font-size: 40px;
  animation: pulse 1.5s infinite;
}

button {
  margin-top: 20px;
  background: linear-gradient(135deg, #e63946, #ff758f);
  border: none;
  color: #fff;
  padding: 12px 22px;
  border-radius: 30px;
  font-size: 16px;
  cursor: pointer;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

button:hover {
  transform: scale(1.05);
  box-shadow: 0 8px 20px rgba(230,57,70,0.4);
}

.hidden {
  display: none;
}

@keyframes pulse {
  0% { transform: scale(1); }
  50% { transform: scale(1.15); }
  100% { transform: scale(1); }
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

  </style>
</head>
<body>
  <div class="card">
    <div class="heart">❤️</div>
    <h1>Oi, você</h1><p>
  Eu fiz isso aqui porque palavras às vezes não cabem só numa conversa.
  Você virou um pensamento bom, daqueles que aparecem sem avisar.
</p>

<p>
  Não é promessa, nem pressão.
  É só verdade.
</p>

<button onclick="mostrarMensagem()">Clica aqui</button>

<p id="mensagem" class="hidden">
  Eu gosto de você. Do seu jeito, do seu sorriso,
  e da paz estranha que você me traz.
  Se um dia isso fizer sentido pra você,
  eu vou gostar de tentar.
</p>

  </div>  <script>
    function mostrarMensagem() {
      document.getElementById('mensagem').classList.remove('hidden');
    }
  </script></body>
</html>
